<%= PageTitle title: t("model_files.general.edit"), breadcrumbs: {
      Model.model_name.human(count: 100) => models_path,
      @file.model.name => model_path(@file.model),
      @file.name => model_model_file_path([@file.model, @file])
    } %>

<%= render "form" %>
