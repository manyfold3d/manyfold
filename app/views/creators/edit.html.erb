<%= PageTitle title: t("creators.general.edit"), breadcrumbs: {
      Creator.model_name.human(count: 100) => creators_path,
      @creator.name => creator_path(@creator)
    } %>

<%= render "form" %>
