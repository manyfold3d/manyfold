<% unless links.empty? %>
  <%= card :secondary, "Links" do %>
    <ul class='links'>
      <% links.each do |link| %>
        <li><%= link_to t("sites.#{link.site}"), link.url %></li>
      <% end %>
    </ul>
  <% end %>
<% end %>
