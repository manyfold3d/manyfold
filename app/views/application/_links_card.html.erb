<% unless links.empty? %>
  <%= card :secondary, Link.model_name.human(count: 100) do %>
    <ul class='links'>
      <%= safe_join(links.map { |it| render Components::Link.new(link: it) }) %>
    </ul>
  <% end %>
<% end %>
