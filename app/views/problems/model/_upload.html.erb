<% model = problem.problematic %>
<%= link_to safe_join([icon("upload", t("application.navbar.upload")), t("application.navbar.upload")], " "), model_path(model, anchor: "upload-form"), class: "btn btn-primary" if policy(model).edit? %>
