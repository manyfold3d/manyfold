<%= if policy(problem.problematic).destroy?
      link_to safe_join([icon("trash", t("general.delete")), t("general.delete")], " "),
        problem.problematic,
        {
          method: "delete",
          data: {confirm: translate("models.destroy.confirm")},
          class: "btn btn-danger"
        }
    end %>
