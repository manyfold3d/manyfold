<% if policy(problem.problematic).show? && policy(problem.problematic).create? && ["stl", "obj"].include?(problem.problematic.extension) %>
  <%= link_to safe_join([icon("arrow-left-right", t("model_files.show.convert")), t("model_files.show.convert")], " "), model_model_files_path(problem.problematic.model, convert: {id: problem.problematic.to_param, to: "threemf"}), method: :post, class: "btn btn-warning" %>
<% end %>
