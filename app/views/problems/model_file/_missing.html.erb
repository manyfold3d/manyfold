<td><%= icon("file-x", t(".title")) %></td>
<td><%= link_to problem.problematic.name, [problem.problematic.model, problem.problematic], class: "link-body-emphasis" %></td>
<td><%= t ".title" %></td>
<td><%= link_to "Purge", [problem.problematic.model, problem.problematic], method: :delete, class: "btn btn-danger" if policy(problem.problematic).destroy? %></td>
