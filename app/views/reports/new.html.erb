<h3><%= t(".title", type: @reportable.model_name.human, name: @reportable.try(:name)) %></h3>

<p class="lead"><%= t ".description" %></p>

<%= form_with model: [@reportable.try(:commentable), @reportable, @report].compact, class: "tabular-form" do |form| %>

  <%= text_input_row form, :content %>

  <div>
    <%= form.submit translate(".submit"), class: "btn btn-primary" %>
  </div>

<% end %>
