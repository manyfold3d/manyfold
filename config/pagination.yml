default: &default
  models: true    # false for infinite scroll
  creators: true  # false for infinite scroll
  per_page: 12

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
