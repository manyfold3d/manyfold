---
ja:
  libraries:
    create:
      failure:
      success:
    destroy:
      success:
    form:
      add_line:
      caption:
        help:
      confirm_destroy:
        help:
      create_path_if_not_on_disk:
        help:
      default:
        help:
      filesystem_description_html:
      folder_organisation:
        description:
        title: フォルダ名式
      icon:
        help_html:
        placeholder:
      notes:
        help_html:
      path:
        placeholder:
      rules:
        title:
      s3_bucket:
        help:
      s3_description_html:
      s3_endpoint:
        help:
      s3_path_style:
        help:
      safe_folder_names:
        help_html: 例：<code>Spın̈al Tap</code>の代わりに<code>spinal-tap</code>
      tag_regex:
        help:
    general:
      edit:
      new:
    index:
      description:
      free_space:
        label:
      heading:
      scan_changes:
        confirm:
      settings:
      show:
        help:
        label:
      tag_regex:
        check:
        search_missing:
    parse_metadata_help:
      help:
    path_template_help:
      description: フォルダ構造は、トークンを使って定義したテンプレートに従います。テンプレートに他のテキストを含めることもでき（フォルダーのセパレーターなど）、そのテキストはそのまま含まれます。
      tokens:
        collection: モデルが入っているコレクションのタイトル（設定されている場合）。
        creator: もし設定されていれば、作成者の名前。
        model_id: モデルの一意な数値識別子。ディスク上での名前の衝突を避けるため、テンプレートの最後に必ず含めることを強くお勧めします。
        model_name: モデル名のファイルシステムに安全なバージョン。
        tags_html: 各タグに1つずつ、タグの人気順に並べられた、入れ子になった一連のフォルダ。例えば、<code>hogehoge/piyopiyp/piyohogeの</code>ように。このトークンは「貪欲」で、できるだけ多くのタグを解析します。
    storage_services:
      filesystem:
      s3:
    update:
      failure:
      success:
